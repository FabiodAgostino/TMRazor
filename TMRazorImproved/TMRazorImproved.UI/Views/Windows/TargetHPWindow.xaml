<Window x:Class="TMRazorImproved.UI.Views.Windows.TargetHPWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:TMRazorImproved.UI.ViewModels"
        mc:Ignorable="d"
        Title="Target HP" Height="50" Width="200"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border CornerRadius="8" 
            Background="#CC000000" 
            BorderBrush="#40FFFFFF" 
            BorderThickness="1"
            Margin="4"
            Visibility="{Binding IsTargetActive, Converter={StaticResource BooleanToVisibilityConverter}}">
        <StackPanel Margin="8,4">
            <ui:TextBlock Text="{Binding TargetName}" FontSize="12" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,4"/>
            <Grid Height="6">
                <ProgressBar Value="{Binding HitsPercent, Mode=OneWay}" Minimum="0" Maximum="1" 
                             Foreground="#FF5555" Background="#33FF5555" BorderThickness="0"/>
            </Grid>
            <ui:TextBlock Text="{Binding Hits, StringFormat={}{0}}" FontSize="10" HorizontalAlignment="Center" Foreground="#FF5555"/>
        </StackPanel>
    </Border>
</Window>
