<ui:FluentWindow x:Class="TMRazorImproved.UI.Views.Windows.MessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight"
        MinWidth="320" MaxWidth="600"
        WindowStartupLocation="CenterOwner"
        WindowBackdropType="Mica"
        ResizeMode="NoResize">

    <Grid Margin="24,16,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Message text -->
        <ui:TextBlock x:Name="MessageText" Grid.Row="0"
                      TextWrapping="Wrap" FontSize="14"
                      Margin="0,0,0,12"/>

        <!-- Optional hyperlink -->
        <TextBlock x:Name="LinkBlock" Grid.Row="1"
                   Visibility="Collapsed" Margin="0,0,0,12">
            <Hyperlink x:Name="LinkAnchor">
                <Run x:Name="LinkRun"/>
            </Hyperlink>
        </TextBlock>

        <!-- Separator -->
        <Separator Grid.Row="2" Margin="0,4,0,12"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <ui:Button x:Name="NoButton"     Visibility="Collapsed" Appearance="Secondary" Margin="0,0,8,0"/>
            <ui:Button x:Name="CancelButton" Visibility="Collapsed" Appearance="Secondary" Margin="0,0,8,0"/>
            <ui:Button x:Name="OkButton"     Appearance="Primary"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
