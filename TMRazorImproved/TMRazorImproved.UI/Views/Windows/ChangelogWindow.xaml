<ui:FluentWindow x:Class="TMRazorImproved.UI.Views.Windows.ChangelogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Changelog" Height="560" Width="700"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <ui:TextBlock x:Name="TitleText" FontSize="22" FontWeight="Bold" Margin="0,0,0,4"/>
            <ui:TextBlock x:Name="VersionText" Appearance="Secondary" FontSize="13"/>
        </StackPanel>

        <Separator Grid.Row="1" Margin="0,0,0,16"/>

        <!-- Changelog content -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <TextBox x:Name="ChangelogContent"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     Background="Transparent"
                     BorderThickness="0"
                     FontFamily="Consolas"
                     FontSize="12"
                     Padding="4"/>
        </ScrollViewer>

        <!-- Footer buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <ui:Button x:Name="OpenOnlineButton"
                       Content="Apri Online"
                       Icon="{ui:SymbolIcon Open24}"
                       Margin="0,0,12,0"
                       Click="OpenOnline_Click"/>
            <ui:Button Content="Chiudi"
                       Appearance="Secondary"
                       Width="100"
                       Click="Close_Click"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
