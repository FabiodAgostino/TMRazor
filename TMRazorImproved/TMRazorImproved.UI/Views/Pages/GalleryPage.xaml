<Page x:Class="TMRazorImproved.UI.Views.Pages.GalleryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:vm="clr-namespace:TMRazorImproved.UI.ViewModels"
      xmlns:loc="clr-namespace:TMRazorImproved.UI.Views"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="{loc:Loc Gallery.Title}"
      d:DataContext="{d:DesignInstance vm:GalleryViewModel}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <StackPanel>
                <TextBlock Text="{loc:Loc Gallery.Header}" Style="{StaticResource TitleTextBlockStyle}" />
                <TextBlock Text="{loc:Loc Gallery.Description}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <ui:Button Content="{loc:Loc Gallery.Refresh}" 
                           ToolTip="{loc:Loc Gallery.Refresh.ToolTip}"
                           Icon="ArrowClockwise24" 
                           Command="{Binding RefreshCommand}" 
                           Margin="0,0,8,0" />
                <ui:Button Content="{loc:Loc Gallery.OpenFolder}" 
                           ToolTip="{loc:Loc Gallery.OpenFolder.ToolTip}"
                           Icon="Folder24" 
                           Appearance="Secondary" 
                           Command="{Binding OpenFolderCommand}" />
            </StackPanel>
        </Grid>

        <!-- Gallery Grid -->
        <ListBox Grid.Row="1" 
                 ItemsSource="{Binding Items}" 
                 SelectedItem="{Binding SelectedItem}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 Background="Transparent" BorderThickness="0">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ui:Card Margin="4" Padding="4" VerticalAlignment="Top">
                                    <StackPanel Width="200">
                                        <Border CornerRadius="4" ClipToBounds="True" Background="{DynamicResource ControlFillColorDefaultBrush}">
                                            <Image Source="{Binding Thumbnail}" Height="150" Stretch="UniformToFill" />
                                        </Border>
                                        <TextBlock Text="{Binding FileName}" 
                                                   Margin="0,8,0,0" 
                                                   FontSize="12" 
                                                   TextTrimming="CharacterEllipsis" 
                                                   HorizontalAlignment="Center" />
                                        <TextBlock Text="{Binding Date, StringFormat='dd/MM/yyyy HH:mm'}" 
                                                   FontSize="10" 
                                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                                                   HorizontalAlignment="Center" />
                                        
                                        <!-- Actions per item -->
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,4">
                                            <ui:Button Icon="Delete16" 
                                                       ToolTip="{loc:Loc Gallery.Delete.ToolTip}"
                                                       Appearance="Secondary" 
                                                       Padding="8,4" 
                                                       Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" 
                                                       CommandParameter="{Binding}" />
                                        </StackPanel>
                                    </StackPanel>
                                </ui:Card>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <!-- Empty State -->
        <Grid Grid.Row="1" Visibility="{Binding Items.Count, Converter={StaticResource IntToVisibilityConverter}, ConverterParameter='0'}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ui:SymbolIcon Symbol="Camera24" FontSize="48" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,16" />
                <TextBlock Text="{loc:Loc Gallery.Empty.Title}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="16" />
                <TextBlock Text="{loc:Loc Gallery.Empty.Hint}" 
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12" HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
