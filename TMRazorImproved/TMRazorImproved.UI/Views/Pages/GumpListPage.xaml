<Page x:Class="TMRazorImproved.UI.Views.Pages.GumpListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Gump List Manager">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,16">
            <ui:TextBlock Text="Gump List Manager" FontSize="28" FontWeight="Bold" />
            <ui:Button Command="{Binding ViewModel.RefreshGumpsCommand}" Icon="{ui:SymbolIcon Refresh24}" Margin="16,0,0,0" ToolTip="Refresh Gumps" />
        </StackPanel>

        <ui:Card Grid.Row="1" Padding="0">
            <DataGrid ItemsSource="{Binding ViewModel.OpenGumps}" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      Background="Transparent" 
                      BorderThickness="0"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Gump ID" Binding="{Binding GumpId, StringFormat=0x{0:X}}" Width="100" />
                    <DataGridTextColumn Header="Serial" Binding="{Binding Serial, StringFormat=0x{0:X}}" Width="100" />
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100" />
                    <DataGridTextColumn Header="Commands" Binding="{Binding Commands.Count}" Width="100" />
                    <DataGridTextColumn Header="Lines" Binding="{Binding Lines.Count}" Width="100" />
                    <DataGridTemplateColumn Header="Actions" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <ui:Button Command="{Binding DataContext.ViewModel.InspectGumpCommand, RelativeSource={RelativeSource AncestorType=Page}}" 
                                               CommandParameter="{Binding}" 
                                               Icon="{ui:SymbolIcon EyeShow24}" 
                                               Content="Inspect" 
                                               Margin="0,0,8,0" />
                                    <ui:Button Command="{Binding DataContext.ViewModel.CloseGumpCommand, RelativeSource={RelativeSource AncestorType=Page}}" 
                                               CommandParameter="{Binding}" 
                                               Icon="{ui:SymbolIcon Dismiss24}" 
                                               Content="Close" 
                                               Appearance="Danger" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </ui:Card>
    </Grid>
</Page>
