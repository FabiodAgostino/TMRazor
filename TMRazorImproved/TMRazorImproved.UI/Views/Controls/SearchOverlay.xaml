<UserControl x:Class="TMRazorImproved.UI.Views.Controls.SearchOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:TMRazorImproved.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Visibility="{Binding IsOverlayVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
    
    <Grid Background="#80000000" MouseDown="OnBackgroundMouseDown">
        <Border Background="{ui:ThemeResource ApplicationBackgroundBrush}"
                BorderBrush="{ui:ThemeResource ControlElevationBorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Width="600"
                MaxHeight="450"
                VerticalAlignment="Top"
                Margin="0,100,0,0"
                Effect="{StaticResource ControlShadow}">
            
            <Grid Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Search Input -->
                <ui:TextBox x:Name="SearchBox"
                            PlaceholderText="Search scripts, macros, agents or pages... (Ctrl+Shift+P)"
                            Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                            Icon="Search24"
                            FontSize="16"
                            KeyDown="OnSearchBoxKeyDown"
                            PreviewKeyDown="OnSearchBoxPreviewKeyDown"/>

                <!-- Results List -->
                <ui:ListView Grid.Row="1"
                             Margin="0,12,0,0"
                             ItemsSource="{Binding SearchResults}"
                             SelectedItem="{Binding SelectedItem}"
                             MaxHeight="350"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ui:ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="40"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <ui:SymbolIcon Symbol="{Binding Icon}" FontSize="24" VerticalAlignment="Center"/>
                                
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0">
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14"/>
                                    <TextBlock Text="{Binding Description}" FontSize="12" Opacity="0.7" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>

                                <ui:Badge Grid.Column="2" Appearance="Secondary" Content="{Binding Category}" VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ui:ListView.ItemTemplate>
                    <ui:ListView.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding ExecuteSelectedCommand}"/>
                    </ui:ListView.InputBindings>
                </ui:ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>
